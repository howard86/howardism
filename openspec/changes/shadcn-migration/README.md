# shadcn-migration

Full monorepo migration from DaisyUI (blog) and Chakra UI (recipe, github-search) to shadcn/ui with Tailwind CSS v4. Preserve existing Japanese color palette and dark mode themes, rewritten as CSS variables. Create shared packages/ui package for monorepo-wide component sharing.
