[default]
extend-ignore-re = [
  "(?Rm)^.*(#|//)\\s*spellchecker:disable-line$",
]

[default.extend-words]
Unparseable = "Unparseable"
PN = "PN"
GAE = "GAE"     # Generalized Advantage Estimation (RL term from wiki content)

[files]
extend-exclude = [
  "*.lock",
  "*.snap",
  "CHANGELOG.md",
  "node_modules",
  ".next",
  ".turbo",
  "apps/blog/public/rss",
  "apps/blog/src/content/**",
  # Generated from the wiki vault; the graph and the backlog now quote article
  # prose, so they carry the same paper jargon the content itself is excluded for.
  "apps/blog/src/data/**",
  "design_handoff_blog_unification",
]
