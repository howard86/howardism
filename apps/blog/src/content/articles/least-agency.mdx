---
date: 2026-05-28
title: Least Agency
description: "OWASP term extending least privilege to agents: constrain not just
  what an agent can access but what each tool can do, how often, and where;
  deny-by-default, per-agent credentials, scope limits"
tag: Concept
domain: ai-engineering
tags:
  - security
  - least-privilege
  - access-control
  - owasp
readingTime: 5
imageAlt: Illustration for Least Agency
sources:
  - title: Zero Trust for AI Agents
    url: https://cdn.prod.website-files.com/6889473510b50328dbb70ae6/6a1611a04085d7cd3dadc924_Claude-eBook-Zero-Trust-for-AI-Agents-05182026.pdf
---
export { default as heroImage } from "../assets/least-agency.png";

## Sources

- [Zero Trust for AI Agents](https://cdn.prod.website-files.com/6889473510b50328dbb70ae6/6a1611a04085d7cd3dadc924_Claude-eBook-Zero-Trust-for-AI-Agents-05182026.pdf)

## Summary

**Least agency** is a term coined by [OWASP](/articles/owasp) that extends the classic least-privilege principle to agentic applications. Where least privilege constrains *what users and systems can access*, least agency goes further: it restricts **what each agent tool can do, how often, and where.** It is the authorization principle at the center of [Zero Trust for AI Agents](/articles/zero-trust-for-ai-agents) — the practical answer to "even a perfectly authenticated agent causes damage when granted excessive permissions."

## The distinction from least privilege

Traditional least privilege is about *access scope* (which resources an identity can touch). Least agency adds the *action and frequency* dimensions that matter once an identity can autonomously chain operations:

- a database tool gets **read-only** queries (not write/delete)
- an email summarizer gets **no send/delete** rights
- an API integration gets **minimal CRUD** operations

The shift matters because an agent operates *within* its granted permissions while still being manipulable into misusing them — the tool-misuse and tool-chaining threats that traditional access controls can't prevent (see [Agentic Prompt Injection](/articles/agentic-prompt-injection) and [Agent Supply Chain Risk](/articles/agent-supply-chain-risk) for the attack side). Least agency shrinks the set of harmful actions reachable *even with valid credentials*.

## How the framework enforces it

Least agency is enforced through the access-control tier ladder and Phase 3/5 of the implementation workflow:

- **Deny-by-default** — block all access not explicitly granted; treat it as a starting posture, not a destination.
- **Permission models** — RBAC (Foundation) → ABAC with context-aware policies (Enterprise) → continuous authorization re-evaluated per action (Advanced).
- **Privilege scoping** — static least-privilege roles (Foundation) → dynamic elevation per task with return-to-baseline (Enterprise) → JIT/JEA with automatic expiration (Advanced). "When in doubt, prefer a control that *removes* a capability over one that *throttles* it."
- **Capability restrictions** — an email tool restricted to reading, with send requiring separate authorization; a database tool that queries but can't change schema.
- **Compartmentalization** — splitting an agent's functions across multiple agents, each with its own unique identity and credentials, so attackers must compromise several agents to reach more resources. *"If you break it into multiple agents and provide them all the same credentials, you have failed to compartmentalize the risk."*

## Relation to blast radius

Least agency is the *input* control; [Blast Radius (Agentic)](/articles/blast-radius) is the *outcome metric*. Constraining agency (actions × frequency × scope) is precisely how you shrink the blast radius an "assume breach" posture expects to be tested. The two are read together: scope an agent's account to read-only on exactly the data it needs, and a stolen credential's blast radius is "severely limited."

## Connections

- [Zero Trust for AI Agents](/articles/zero-trust-for-ai-agents) — the framework that adopts least agency as its authorization principle (hub)
- [Blast Radius (Agentic)](/articles/blast-radius) — least agency is how you constrain it; agency-in, blast-radius-out
- [Agent Identity and Authentication](/articles/agent-identity-and-authentication) — least agency is unenforceable without distinct per-agent identity (the "attribution gap" makes enforcement impossible)
- [OWASP](/articles/owasp) — coined the term
- [Claude Code Best Practices](/articles/claude-code-best-practices) — Claude Code's deny-by-default permissions and write-access restrictions are the cited reference implementation
- [Agentic Prompt Injection](/articles/agentic-prompt-injection) — the attack that least agency contains: manipulated agents misusing tools within authorized privileges
- [Impossible, Not Tedious (Design Test)](/articles/impossible-not-tedious-test) — "prefer removing a capability over throttling it" is least agency expressed as a design heuristic
- [MCP and Computer Use](/articles/mcp-and-computer-use) — capability restrictions per MCP tool (read-only, no-send) are least agency at the tool-interface level
- [Autonomous Defense](/articles/autonomous-defense) — least agency applied *inward* on defensive agents: scoped automated-response actions with clear boundaries
- [Agent Supply Chain Risk](/articles/agent-supply-chain-risk) — scoping what a (possibly poisoned) tool can do limits the damage a compromised dependency can cause
- [Out-of-Band Prompt-Injection Defense](/articles/out-of-band-prompt-injection-defense) — Progent enforces least agency *deterministically* at the tool-call boundary (symbolic per-call privilege rules); the reference monitor is the mechanism that makes "restrict what each tool can do" a hard barrier rather than a prompt advisory
- [Agent Identity Management System (AIMS)](/articles/agent-identity-management-system) — AIMS enforces least agency at the standards layer: OAuth minimal scopes + audience restriction, and **transaction tokens** that downscope a broad access token to a single non-reusable transaction along the microservice call chain
- [Foundation → Enterprise → Advanced: Is the Agent Access-Control Jump a Cliff?](/articles/agent-access-control-tier-migration) — where the RBAC→ABAC→continuous-authorization and static→dynamic→JIT ladders sit; the Enterprise midpoint and the ABAC-tier-placement contradiction

## Open Questions

- Least agency adds a *frequency* dimension ("how often"), but the framework also says rate limits are friction, not barriers ([Impossible, Not Tedious (Design Test)](/articles/impossible-not-tedious-test)). How is frequency-limiting both a least-agency control and a friction-only one — context-dependent?
- Dynamic privilege elevation (Enterprise) reintroduces an elevation path; how is the elevation request itself authenticated against a manipulated agent?

## Sources

- [Zero Trust for AI Agents](https://cdn.prod.website-files.com/6889473510b50328dbb70ae6/6a1611a04085d7cd3dadc924_Claude-eBook-Zero-Trust-for-AI-Agents-05182026.pdf) — least agency defined in Part I; enforced across Part III access-control tiers and Part IV Phases 3 & 5
